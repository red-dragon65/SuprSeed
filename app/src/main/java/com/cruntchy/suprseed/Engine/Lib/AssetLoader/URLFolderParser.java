package com.cruntchy.suprseed.Engine.Lib.AssetLoader;


/**
 * Example for getting file paths for folder items with an online url
 */
public class URLFolderParser implements FolderParser {


    @Override
    public String[] getSubPaths(String basePath) {

        /*

        Open connection

        Socket connection = new Socket();

        String filePaths;

        while (connection.hasNext()){

            filePath = connection.getUrl();
        }

         */

        return new String[0];
    }
}
